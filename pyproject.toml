[build-system]
requires = [
    "setuptools",
]
build-backend = "setuptools.build_meta"

[project]
name = "dbd-maps"
version = "0.0.0"
description = "Dead by Daylight map overlay"
requires-python = ">=3.9"
dependencies = [
    "Pillow",
    "mss",
    "keyboard",
    "winocr; sys_platform == 'win32'",
]

[project.optional-dependencies]
test = [
    "pytest",
]

[tool.setuptools.packages.find]
where = ["src"]